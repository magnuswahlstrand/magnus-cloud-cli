{
  "name": "magnus-cloud-cli",
  "version": "1.0.1",
  "description": "prototype for authentication from the terminal",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "npm run build && node dist/index.js"
  },
  "bin": "dist/index.js",
  "author": "",
  "license": "ISC",
  "keywords": [
    "cli",
    "npmtool"
  ],
  "dependencies": {
    "@clack/prompts": "^0.7.0",
    "picocolors": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.5.1"
  }
}
